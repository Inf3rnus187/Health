/// <reference types="vite/client" />

/** When this build was made (also in /version.json). */
declare const __BUILD_ID__: string;

/** The commit this build was made from ('' when not given). */
declare const __COMMIT__: string;
